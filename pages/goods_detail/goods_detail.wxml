<!--pages/goods_detail/goods_detail.wxml-->
<van-card
  desc="{{goods.standard}}"
  title="{{goods.name}}"
  price="{{goods.act_price}}"
  origin-price="{{item.price}}"
  thumb="{{ imageURL }}"
/>
<van-submit-bar
  price="{{ 3050 }}"
  button-text="提交订单"
  bind:submit="onSubmit"
/>
